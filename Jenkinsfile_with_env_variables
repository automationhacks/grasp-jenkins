/* https://www.jenkins.io/doc/pipeline/tour/agents/*/
pipeline {
    agent {
        docker {
            image 'node:18.16.0-alpine'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}